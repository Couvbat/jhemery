node_modules
dist

# secrets
backend/.env
frontend/.env

#builds
backend/dist
frontend/dist

# superpowers scratch state (subagent-driven-development progress ledger, etc.)
.superpowers/

# graft's local graph cache — regenerable, not committed (run `graft build`).
# Anchored: an unanchored `graft/` also swallows .claude/skills/graft/.
/graft/
